// Exemplo 2, indentificando o tipo de variavel

const valor = 50.5; // Contante tipo double
const num = 100; // Constante tipo int
const a = true; // constante do tipo booleana

// Printando os valores no console
console.log(valor, `Tipo da constante ${typeof valor}`);
console.log(num, `${typeof num}`);
console.log(a,`${typeof a}`);